import Container from "../Container";

function Contacts() {
  return (
    <section id="contacts" className="py-20">
      <Container>
        <div>Contacts (Contact us)</div>
      </Container>
    </section>
  );
}

export default Contacts;
